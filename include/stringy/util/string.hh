#pragma once

#include <texy/util/string.hh>

namespace stringy
{
    using texy::forceAddScope;
    using texy::addScope;
    using texy::addStrictScope;
    using texy::addTexScope;
    using texy::addAllScopes;
    using texy::multiplyIfNotEmpty;
}
